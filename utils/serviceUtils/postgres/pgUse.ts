// import { Postgres } from "./postgresUtil";

// async function main() {
//   const pg = new Postgres();

//   await pg.connect("commondb");
//   //   const res = await pg.execute("select count(id),id from ab group by id", []);
//   //   const res = await pg.execute("update ab set id = $1 where id=$2 ", [
//   //     "456",
//   //     "123",
//   //   ]);

//   //   const res = await pg.execute("insert into ab(id) values($1)", ["777"]);

//   console.log("res is ", res);
// }

// // main();
